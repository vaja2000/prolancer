import { ScrollNavbarBgDirective } from './scroll-navbar-bg.directive';

describe('ScrollNavbarBgDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollNavbarBgDirective();
    expect(directive).toBeTruthy();
  });
});
